import styled from '@emotion/styled';

// Container for device positioning
export const DeviceContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  min-width: 100vw;
  background: linear-gradient(to top, rgb(61, 31, 0), rgb(96, 48, 1));
  padding: 20px;
`; 